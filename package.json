{
  "name": "deepx-desktop",
  "version": "0.9.0",
  "description": "DeepX desktop client",
  "author": "DeepX",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.15.0",
  "main": "./out/main/main.js",
  "scripts": {
    "dev": "electron-vite dev",
    "build": "pnpm typecheck && electron-vite build",
    "start": "electron-vite preview",
    "prepare:daemon": "node scripts/prepare-daemon.mjs",
    "prepare:daemon:local": "node scripts/prepare-daemon.mjs --backend-root ../DeepX",
    "package:dir": "pnpm prepare:daemon && pnpm build && electron-builder --dir --win --x64 --publish never",
    "package:dir:local": "pnpm prepare:daemon:local && pnpm build && electron-builder --dir --win --x64 --publish never",
    "package:win": "pnpm prepare:daemon && pnpm build && electron-builder --win nsis --x64 --publish never",
    "package:win:local": "pnpm prepare:daemon:local && pnpm build && electron-builder --win nsis --x64 --publish never",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@solidjs/web": "2.0.0-beta.21",
    "@tailwindcss/vite": "^4.3.0",
    "ansi-to-html": "^0.7.2",
    "diff2html": "^3.4.56",
    "katex": "^0.18.1",
    "lightweight-charts": "^5.2.0",
    "marked": "^18.0.5",
    "marked-shiki": "^1.2.1",
    "shiki": "^4.3.1",
    "solid-js": "2.0.0-beta.21",
    "tailwindcss": "^4.3.0",
    "ws": "^8.21.1"
  },
  "devDependencies": {
    "@types/katex": "^0.16.8",
    "@types/node": "^24.0.0",
    "@types/ws": "^8.18.1",
    "electron": "^43.2.0",
    "electron-builder": "^26.15.3",
    "electron-vite": "^5.0.0",
    "jsdom": "^29.1.1",
    "typescript": "~6.0.3",
    "vite": "^8.0.0",
    "vite-plugin-solid": "3.0.0-next.14",
    "vitest": "^4.1.10"
  }
}
